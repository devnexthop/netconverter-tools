# Optional: copy to .env for convenience (never commit .env)
# All values are required unless you pass flags on the command line instead.
export FMC_HOST=10.0.0.1
export FMC_USER=api_readonly
export FMC_PASSWORD=
